
#include <stdio.h>

int main() {
    int a[10],pos,value, n=8;
    printf("Enter the Element In Array");
    for(int i=0;i<n;i++){
        scanf("%d", &a[i]);
    }
    printf("Enter The The Position In Which You To Delete");
    scanf("%d",&pos);
    
    for(int i=pos-1; i<n-1; i++){
        a[i]=a[i+1];
    }
    n--;
    printf("Array after The Deletions no of element");
        for(int i=0;i<10;i++){
            printf("%d",a[i]);
        }
        return 0;
}
